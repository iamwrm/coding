#include <iostream>

using namespace std;

#define MAX_DIGITS 100

class LongNum {
	int digits[MAX_DIGITS];

       public:
	LongNum times(LongNum &ln1);
};

int main(int argc, char const *argv[])
{
	string oneline;
	while (getline(cin, oneline)) {
		LongNum templn;
		int expNum;
	}
	return 0;
}